server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    root /srv/uwc-hpc-skills/current;
    index index.html;

    location = /healthz {
        access_log off;
        return 204;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
